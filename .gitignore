/target
**/target
node_modules
dist
src-tauri/gen
.DS_Store
Thumbs.db

# Test photographs. Personal data: never commit these.
/test-data

# ONNX Runtime library (11MB). Downloaded per machine, see runtime/README.md.
# The model weights in models/ are committed: they are small and the build
# depends on their exact contents.
/runtime/*.dll

# Throwaway toolchain verification project
/_toolchain_probe

# Machine-local cargo settings (contains an absolute target-dir path).
# See .cargo/config.toml.example for why it exists and how to recreate it.
/.cargo/config.toml
