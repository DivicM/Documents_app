/target
**/target
node_modules
dist
src-tauri/gen
.DS_Store
Thumbs.db

# Test photographs. Personal data: never commit these.
/test-data

# Large binaries fetched per machine by fetch-models.ps1. The BiRefNet model is
# 109MB, over GitHub's 100MB file limit; the ONNX Runtime DLL is identical for
# everyone. YuNet (227KB) stays committed: it is small and the build depends on
# its exact contents.
/runtime/*.dll
/models/birefnet_lite_fp16.onnx

# Throwaway toolchain verification project
/_toolchain_probe

# Machine-local cargo settings (contains an absolute target-dir path).
# See .cargo/config.toml.example for why it exists and how to recreate it.
/.cargo/config.toml
